# Optional — override the Atlassian MCP endpoint (defaults to v1/mcp).
# No client id/secret needed: auth is a one-time browser consent against
# Atlassian's own pre-registered mcp-remote OAuth client.
# MCP_URL=https://mcp.atlassian.com/v1/mcp
